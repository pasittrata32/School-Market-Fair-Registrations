
export enum Language {
  TH = 'th',
  EN = 'en',
}

export enum RegistrantType {
  External = 'external',
  Parent = 'parent',
  Student = 'student',
}

export interface ProductItem {
  id: number;
  name: string;
}
